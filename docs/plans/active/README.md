# Active Plans

- `m06-plan-host-compatibility-pass.md` — verify the M04/M05 typing controls
  across the real desktop hosts (Chromium, Electron editor, chat, rich-edit,
  console), fix in-scope defects (notably the deferred lone-Shift
  `WH_KEYBOARD_LL` fallback), and capture host-matrix evidence.

M01 through M05 have moved to `docs/plans/history/`. See
`docs/roadmap.md` for candidate next milestones.
