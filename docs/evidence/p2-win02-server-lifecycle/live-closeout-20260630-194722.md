# P2-WIN02 Live Closeout Attempt - 2026-06-30 19:47

Status: failed-pending-reboot-and-rerun

Fresh approval was used for the resumed live path:

- approval note: `User approved P2-WIN02 live install/register/smoke/uninstall/cleanup in this session on 2026-07-01.`
- launcher transcript:
  `docs/evidence/p2-win01-installer/elevated-live-smoke-transcript-20260630-194722.txt`
- launcher result:
  `docs/evidence/p2-win01-installer/elevated-live-smoke-launch-result.json`
- closeout audit:
  `docs/evidence/p2-win01-closeout/audit.json`

The run reached install/register and profile activation. The Notepad smoke then
failed before Chromium or diagnostics:

- Notepad result:
  `docs/evidence/p2-win01-tsf-smoke/notepad-smoke-result.md`
- `profile_active_verified_before_typing: True`
- `product_owned_server_start_observed: True`
- `product_owned_server_ready_observed: True`
- observed committed text: `我`
- expected committed text: `我係個`
- structural summary:
  `candidate_update=7, commit_request=1, commit_text=1, key_down=7`

This is a different failure than the earlier cold-readiness timeout. The live
evidence proves the installed TSF DLL started the product-owned server and the
patched smoke waited for readiness. The candidate screenshot showed the real
input path still composing `ngo5` / `我`, and the structural log recorded eight
buffered key-down events before the commit. The server-launch probe `n` was
still present in the IME composition buffer because the target reset cleared
text-field contents but did not cancel active IME composition.

Follow-up fix in this branch:

- added `Cancel-YuneWindowsTextComposition` in `tools/live-smoke-support.ps1`;
- changed Notepad and Chromium smokes to send Escape after the product-owned
  server launch probe and before the post-ready target reset/retype path;
- updated `tools/test-product-owned-server-smoke-contract.ps1` to require the
  composition-cancel helper and the exact `composition cancel after server
  launch probe` call-site text in both smokes;
- fixed empty failure-screenshot-error evidence rendering so
  `git diff --check` stays clean after failed live runs.

Verification after the fix:

- `tools/test-product-owned-server-smoke-contract.ps1` failed RED on the
  missing composition-cancel helper, then passed after the fix;
- focused contracts passed:
  `tools/test-cold-profile-selection-contract.ps1`,
  `tools/test-start-server-readiness-contract.ps1`, and
  `tools/test-tsf-server-cold-start-readiness-contract.ps1`;
- the non-elevated build/smoke/contract suite from the active P2-WIN02 plan
  passed;
- `tools/audit-p2-win01-closeout.ps1` wrote an incomplete audit, as expected;
- `git diff --check` passed.

Cleanup scheduled delayed deletes under
`C:\Users\laubonghaudoi\AppData\Local\Yune\WindowsIme` and requires sign-out or
reboot before another live closeout attempt. Structured cleanup result:

- `requires_reboot: true`
- `pending_delete_scheduled: true`
- locked module holders recorded:
  `Codex`, `explorer`, `L-Connect 3`, `NVIDIA Overlay`, `steamwebhelper`
- pending-delete paths are all under the approved install root.

Do not mark P2-WIN02 or dogfood readiness complete from this run. The next live
step is sign-out/reboot, verify the install directory and pending-delete entries
are gone, then rerun one approved full live closeout with the patched
composition-cancel gate.
